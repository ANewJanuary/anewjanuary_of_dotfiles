# https://wiki.hyprland.org/Configuring/
# source = ~/.config/hypr/myColors.conf

### MONITORS ###
monitor=DP-3,2560x1440@144,auto,1.25
monitor=HDMI-A-1,1920x1080@60,auto,auto

### MY PROGRAMS ###
# https://wiki.hyprland.org/Configuring/Keywords/

$terminal = alacritty
$fileManager = thunar
$menu = wofi --show drun
$browser = flatpak run com.brave.Browser

### AUTOSTART ###
# exec-once = $terminal
# exec-once = nm-applet &
exec-once = waybar & hyprpaper
exec-once = mako
exec-once = systemctl --user start hyprpolkitagent
exec-once = hyprpm reload -n
exec-once = hyprpaper
exec-once = syncthing
exec-once = systemctl --user start hyprpolkitagent
exec-once = mpd
# exec-once = alacritty -e zsh -c "nvim ~/Vshrd/Vault/Index/agenda.md"
# exec-once = alacritty -e zsh -c "nvim ~/Vshrd/Vault/Index/dlink.md"
exec-once = alacritty -e zsh -c "dooit --db /home/artin/Vshrd/Vault/General/agenda.db"
exec-once= waypaper --restore
### ENVIRONMENT VARIABLES ###
# https://wiki.hyprland.org/Configuring/Environment-variables/

### PERMISSIONS ###
# https://wiki.hyprland.org/Configuring/Permissions/

ecosystem {
		# enforce_permissions = 1
}

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

# unscale XWayland
xwayland {
  force_zero_scaling = true
}

# toolkit-specific scale
env = GDK_SCALE,2
env = XCURSOR_SIZE,32
plugin {
		split-monitor-workspaces {
				count = 10
						keep_focused = 0
						enable_notifications = 0
						enable_persistent_workspaces = 1
		}
}
### LOOK AND FEEL ###
# https://wiki.hyprland.org/Configuring/Variables/
# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 1
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types 
    # col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.active_border = rgba(ffff99ee)
    col.inactive_border = rgba(595959aa)
    resize_on_border = false
    # https://wiki.hyprland.org/Configuring/Tearing/ 
    allow_tearing = false
    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0

    active_opacity = 1.0
    inactive_opacity = 0.8

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 4
        passes = 3
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes 

    # https://wiki.hyprland.org/Configuring/Animations/

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# https://wiki.hyprland.org/Configuring/Workspace-Rules/
# https://wiki.hyprland.org/Configuring/Master-Layout/ 
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 
    disable_hyprland_logo = true 
}

### INPUT ###
# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures

# Example per-device config
# https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs 
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


### KEYBINDINGS ###
# https://wiki.hyprland.org/Configuring/Keywords/
# https://wiki.hyprland.org/Configuring/Binds/ 
$mainMod = SUPER 
bind = $mainMod, return, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod, F, exec, $fileManager
bind = $mainMod, R, exec, $menu
bind = $mainMod, B, exec, $browser
bind = $mainMod, P, exec, grim -g "$(slurp)" - | swappy -f - 
# bind = $mainMod, I, exec, bash /home/artin/Vshrd/shell-scripts/macros.sh
bind = $mainMod, I, exec, [float] alacritty -e zsh -c "bash /home/artin/Vshrd/shell-scripts/macros.sh"
bind = $mainMod SHIFT, I, exec, rofimoji
bind = $mainMod, N, exec, [float] alacritty -e zsh -c "bash /home/artin/Vshrd/shell-scripts/lppath.sh n"
bind = $mainMod SHIFT, N, exec, [float] alacritty -e zsh -c "bash /home/artin/Vshrd/shell-scripts/lppath.sh p"
bind = $mainMod, S, exec, bash /home/artin/Vshrd/shell-scripts/date.sh
bind = $mainMod, T, exec, bash /home/artin/Vshrd/shell-scripts/ezlayout.sh
bind = $mainMod, W, exec, killall waybar
bind = $mainMod SHIFT, W, exec, waybar
bind = $mainMod, C, exec, [float] alacritty
bind = $mainMod, a, exec, [float] alacritty -e zsh -c "dooit --db /home/artin/Vshrd/Vault/General/agenda.db"
# bind = $mainMod, N, exec, [float] alacritty -e zsh -c "bash /home/artin/Vshrd/shell-scripts/agenda.sh"

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

#screen lock
bindl=,switch:off:Lid Switch, exec, hyprlock --immediate

# Music playerctl stuff
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous


bind = $mainMod SHIFT, right, exec, playerctl next
bind = $mainMod SHIFT, up, exec, playerctl play-pause
bind = $mainMod SHIFT, down, exec, playerctl play-pause
bind = $mainMod SHIFT, left, exec, playerctl previous

### WINDOWS AND WORKSPACES ###
# https://wiki.hyprland.org/Configuring/Window-Rules/
# https://wiki.hyprland.org/Configuring/Workspace-Rules/

# Aesthetic stuff
windowrule = opacity 1 1, class:^(Alacritty)
windowrule = opacity 0.8 0.8, class:^(wofi)
windowrulev2 = opacity 0.9 0.9, class:^(brave-browser)$   
layerrule = blur,class^(waybar)
layerrule = blur,class^(wofi)
xwayland {
  force_zero_scaling = true
}
# Ignore maximize requests 
windowrule = suppressevent maximize, class:.*
# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
